name: Release App
on:
  push:
    branches:
      - master
# jobs are run in parallel on different machines
# all steps run in series
jobs:
  release:
    name: Release App
    runs-on: ubuntu-latest
    steps:
        # workaround to specify branch AND tag
        # https://stackoverflow.com/questions/59874409/trigger-github-workflow-only-if-pushed-to-specific-branch-and-tag-exists
      - name: Exit if not tag
        if: startsWith(github.ref, 'refs/tags/') == false
        run: exit -1

      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin' #Eclipse
          java-version: '17'
      - name: Make gradlew executable
        run: chmod +x ./gradlew
      - name: Run Linters and Test
        run: ./gradlew check
      - name: Zip Test Results
        uses: montudor/action-zip@v1
        with:
          args: zip -qq -r test-reports.zip app/build/reports
      - name: Build with Gradle
        run: ./gradlew build
      - name: Build Release AAB
        run: ./gradlew bundleRelease
      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace
      - name: Build Release APK
        run: ./gradlew assembleRelease --stacktrace
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          name: Release ${{ github.ref_name }}
          files: |
            app/build/outputs/bundle/release/app-release.aab
            app/build/outputs/apk/release/app-release-unsigned.apk
            app/build/outputs/apk/debug/app-debug.apk
            test-reports.zip